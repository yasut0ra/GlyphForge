# Optional. Without this key the app uses the local heuristic planner and
# offline procedural reference images; uploaded images always work.
OPENAI_API_KEY=
OPENAI_PLANNER_MODEL=gpt-5.4-mini
OPENAI_IMAGE_MODEL=gpt-image-1
